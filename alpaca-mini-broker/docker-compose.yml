services:
  web:
    build: .
    container_name: alpaca-mini-broker
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data: